rootProject.name = 'kitchenpos'
include ':commons'
include ':product'
include ':menu'
include ':table'
include ':order'

for (project in rootProject.children) {
    project.projectDir = file("sub-modules/${project.name}")
    project.buildFileName = "${project.name}.gradle"
}